# Pipeline HRFlow — Théo Marchand oct 2021
# TODO: ajouter tests quand on en aura

name: Deploy to Production

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: '16'
      - run: npm install
      - run: cd frontend && npm run build
      # Tests désactivés — on n'en a pas encore
      # - run: npm test
      - name: Deploy via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.PROD_HOST }}
          username: deploy
          password: ${{ secrets.PROD_SSH_PASSWORD }}
          script: |
            cd /var/www/hrflow
            git pull origin main
            npm install --production
            pm2 restart all
