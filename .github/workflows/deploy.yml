name: HRFlow Deploy

on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string

permissions:
  contents: read

# Sans ça, chaque push déclenche son propre run complet (pipeline.yml se lance sur
# tout push) et rien n'empêche plusieurs déploiements de tourner en parallèle sur le
# même environnement : ils se marchent dessus sur le même state Terraform et le même
# cluster k8s, et le dernier à terminer "gagne", même s'il correspond à un commit plus
# ancien. On annule les runs devenus obsolètes pour ce même environnement au profit du
# plus récent.
concurrency:
  group: hrflow-deploy-${{ inputs.environment }}
  cancel-in-progress: true

env:
  AWS_REGION: ${{ secrets.AWS_REGION }}

jobs:
  # ============================================================
  # Provisionne/maintient à jour l'infra Blue-Green (2 EC2 k3s + 1 ALB avec
  # une target group par couleur + 1 RDS partagée). Cet apply ne bascule
  # JAMAIS le trafic : il repasse toujours la couleur DEJA active (lue dans
  # le state avant apply), donc c'est un apply "de synchro" pur (AMI,
  # instance_type, etc.). La bascule elle-même n'a lieu que dans le job
  # switch-traffic, une fois la couleur idle déployée et smoke-testée.
  # ============================================================
  terraform:
    name: Provision infrastructure (blue + green)
    runs-on: ubuntu-24.04
    environment: ${{ inputs.environment }}
    outputs:
      instance_id_blue: ${{ steps.tf-output.outputs.instance_id_blue }}
      instance_id_green: ${{ steps.tf-output.outputs.instance_id_green }}
      rds_endpoint: ${{ steps.tf-output.outputs.rds_endpoint }}
      rds_address: ${{ steps.tf-output.outputs.rds_address }}
      db_name: ${{ steps.tf-output.outputs.db_name }}
      db_username: ${{ steps.tf-output.outputs.db_username }}
      alb_dns_name: ${{ steps.tf-output.outputs.alb_dns_name }}
      active_color: ${{ steps.tf-output.outputs.active_color }}
      idle_color: ${{ steps.tf-output.outputs.idle_color }}
    env:
      TF_VAR_database_password: ${{ secrets.DB_PASSWORD }}
      TF_VAR_ssh_allowed_cidr: ${{ secrets.SSH_ALLOWED_CIDR }}
      TF_VAR_certificate_arn: ${{ vars.ACM_CERTIFICATE_ARN }}
      TF_VAR_alert_email: ${{ vars.ALERT_EMAIL }}

    steps:
      - name: Checkout source
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@7474bc4690e29a8392af63c5b98e7449536d5c3a # v4
        with:
          aws-region: ${{ secrets.AWS_REGION }}
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@b9cd54a3c349d3f38e8881555d616ced269862dd # v3
        with:
          # Le wrapper par défaut ajoute une annotation "::error::Terraform
          # exited with code N." sur STDOUT (pas stderr) dès qu'une commande
          # terraform échoue — ça pollue silencieusement tout `$(terraform
          # output ...)` capturé en shell, même avec `2>/dev/null` (qui ne
          # redirige que stderr). Désactivé partout ici car plusieurs steps
          # dépendent d'une capture stdout propre (ex: fallback "blue" si
          # active_color n'existe pas encore dans le state).
          terraform_wrapper: false

      - name: Terraform init & select workspace
        working-directory: ./terraform
        run: |
          terraform init
          terraform workspace select "${{ inputs.environment }}" || \
            terraform workspace new "${{ inputs.environment }}"

      - name: Terraform Validate
        working-directory: ./terraform
        run: terraform validate

      # Doit tourner AVANT le plan/apply : sans ça, ne pas passer -var
      # "active_color" referait retomber l'apply sur le default de la
      # variable ("blue"), ce qui basculerait le trafic silencieusement à
      # chaque simple sync d'infra. Le fallback "blue" ne sert qu'au tout
      # premier apply (aucun state/output n'existe encore).
      - name: Determine current active color
        id: color
        working-directory: ./terraform
        run: |
          # Pas de `|| echo blue` sur la même ligne que la capture : si la
          # commande échoue (1er apply, "active_color" n'existe pas encore
          # dans le state), on écrase explicitement plutôt que de risquer de
          # concaténer une sortie parasite dans ACTIVE_COLOR (ce qui casserait
          # ensuite l'écriture dans $GITHUB_ENV, cf. incident précédent).
          if ACTIVE_COLOR="$(terraform output -raw active_color 2>/dev/null)" \
             && { [ "$ACTIVE_COLOR" = "blue" ] || [ "$ACTIVE_COLOR" = "green" ]; }; then
            echo "Couleur active actuelle (lue dans le state) : $ACTIVE_COLOR"
          else
            ACTIVE_COLOR=blue
            echo "Pas de couleur active dans le state (1er apply Blue-Green) : défaut = $ACTIVE_COLOR"
          fi
          echo "ACTIVE_COLOR=${ACTIVE_COLOR}" >> "$GITHUB_ENV"

      - name: Terraform Plan
        working-directory: ./terraform
        run: |
          terraform plan \
            -var-file="${{ inputs.environment }}.tfvars" \
            -var="active_color=${ACTIVE_COLOR}"

      - name: Terraform Apply
        working-directory: ./terraform
        run: |
          terraform apply -auto-approve \
            -var-file="${{ inputs.environment }}.tfvars" \
            -var="active_color=${ACTIVE_COLOR}"

      - name: Get terraform outputs
        id: tf-output
        working-directory: ./terraform
        run: |
          set -euo pipefail
          # Un output vide (ex: mauvais workspace, apply partiel) ne doit jamais
          # continuer silencieusement.
          for name in instance_id_blue instance_id_green rds_endpoint rds_address \
                      db_name db_username alb_dns_name active_color; do
            value="$(terraform output -raw "$name")"
            if [ -z "$value" ]; then
              echo "::error::terraform output '$name' est vide (workspace: $(terraform workspace show))." >&2
              exit 1
            fi
            echo "${name}=${value}" >> "$GITHUB_OUTPUT"
          done

          ACTIVE_COLOR="$(terraform output -raw active_color)"
          if [ "$ACTIVE_COLOR" = "blue" ]; then IDLE_COLOR=green; else IDLE_COLOR=blue; fi
          echo "idle_color=${IDLE_COLOR}" >> "$GITHUB_OUTPUT"

  # ============================================================
  # Déploie la nouvelle version sur le cluster k3s de la couleur IDLE
  # (celle qui ne sert pas le trafic public). Reprend telle quelle la
  # logique de déploiement k8s existante (SSM, Traefik, ConfigMap/Secret,
  # postgres-init, services, ingress) — seule différence : la cible SSM est
  # l'instance idle, pas "l'unique" instance d'avant.
  # ============================================================
  deploy-idle:
    name: Deploy application to k3s (idle)
    runs-on: ubuntu-24.04
    needs: terraform
    environment: ${{ inputs.environment }}
    env:
      NAMESPACE: hrflow-${{ inputs.environment }}
      REPLICAS: ${{ inputs.environment == 'production' && '2' || '1' }}
    steps:
      - name: Checkout source
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@7474bc4690e29a8392af63c5b98e7449536d5c3a # v4
        with:
          aws-region: ${{ secrets.AWS_REGION }}
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@b9cd54a3c349d3f38e8881555d616ced269862dd # v3
        with:
          # Le wrapper par défaut ajoute une annotation "::error::Terraform
          # exited with code N." sur STDOUT (pas stderr) dès qu'une commande
          # terraform échoue — ça pollue silencieusement tout `$(terraform
          # output ...)` capturé en shell, même avec `2>/dev/null` (qui ne
          # redirige que stderr). Désactivé partout ici car plusieurs steps
          # dépendent d'une capture stdout propre (ex: fallback "blue" si
          # active_color n'existe pas encore dans le state).
          terraform_wrapper: false

      # needs.terraform.outputs.* peut arriver vide ici même quand le job terraform
      # a réussi avec des valeurs non vides (cf actions/runner#2477, intermittent).
      # On relit directement le state Terraform (backend S3 distant, déjà persisté
      # par le job terraform) plutôt que de dépendre de ce mécanisme.
      - name: Get terraform outputs from state
        working-directory: ./terraform
        run: |
          set -euo pipefail
          terraform init -input=false
          terraform workspace select "${{ inputs.environment }}"

          ACTIVE_COLOR="$(terraform output -raw active_color)"
          if [ "$ACTIVE_COLOR" = "blue" ]; then IDLE_COLOR=green; else IDLE_COLOR=blue; fi
          echo "ACTIVE_COLOR=${ACTIVE_COLOR}" >> "$GITHUB_ENV"
          echo "IDLE_COLOR=${IDLE_COLOR}" >> "$GITHUB_ENV"

          declare -A tf_to_env=(
            [rds_endpoint]=RDS_ENDPOINT
            [rds_address]=RDS_ADDRESS
            [db_name]=DB_NAME
            [db_username]=DB_USERNAME
            [alb_dns_name]=ALB_DNS_NAME
            ["instance_id_${IDLE_COLOR}"]=INSTANCE_ID
          )
          for tf_name in "${!tf_to_env[@]}"; do
            env_name="${tf_to_env[$tf_name]}"
            value="$(terraform output -raw "$tf_name")"
            if [ -z "$value" ]; then
              echo "::error::terraform output '$tf_name' est vide (workspace: $(terraform workspace show))." >&2
              exit 1
            fi
            echo "${env_name}=${value}" >> "$GITHUB_ENV"
          done
          echo "Déploiement de la couleur idle ($IDLE_COLOR, active actuelle : $ACTIVE_COLOR) sur l'instance $INSTANCE_ID"

      - name: Setup kubectl
        uses: azure/setup-kubectl@3e0aec4d80787158d308d7b364cb1b702e7feb7f # v4

      - name: Wait for SSM agent to register
        run: |
          echo "Waiting for instance to be managed by SSM..."
          for i in $(seq 1 30); do
            STATUS=$(aws ssm describe-instance-information \
              --filters "Key=InstanceIds,Values=$INSTANCE_ID" \
              --query "InstanceInformationList[0].PingStatus" --output text 2>/dev/null)
            if [ "$STATUS" == "Online" ]; then
              echo "SSM agent is online."
              break
            fi
            echo "Attempt $i/30: SSM agent not ready yet (status: $STATUS)"
            sleep 10
          done

      - name: Wait for k3s installation to complete
        run: |
          COMMAND_ID=$(aws ssm send-command \
            --instance-ids "$INSTANCE_ID" \
            --document-name "AWS-RunShellScript" \
            --parameters 'commands=[
              "for i in $(seq 1 60); do",
              "  if [ -f /etc/rancher/k3s/k3s.yaml ]; then echo K3S_READY; exit 0; fi",
              "  echo Attempt $i/60: waiting for k3s...",
              "  sleep 15",
              "done",
              "echo TIMEOUT waiting for k3s",
              "exit 1"
            ]' \
            --query "Command.CommandId" --output text)

          aws ssm wait command-executed \
            --command-id "$COMMAND_ID" \
            --instance-id "$INSTANCE_ID" || true

          STATUS=$(aws ssm get-command-invocation \
            --instance-id "$INSTANCE_ID" \
            --command-id "$COMMAND_ID" \
            --query "Status" --output text)

          aws ssm get-command-invocation \
            --instance-id "$INSTANCE_ID" \
            --command-id "$COMMAND_ID" \
            --query "StandardOutputContent" --output text

          if [ "$STATUS" != "Success" ]; then
            echo "ERREUR : k3s n'a pas fini de s'installer à temps (status: $STATUS)."
            echo "--- fin de /var/log/novatech-install.log sur l'instance ---"
            LOG_CMD_ID=$(aws ssm send-command \
              --instance-ids "$INSTANCE_ID" \
              --document-name "AWS-RunShellScript" \
              --parameters 'commands=["tail -n 100 /var/log/novatech-install.log || echo LOG_NOT_FOUND"]' \
              --query "Command.CommandId" --output text)
            aws ssm wait command-executed --command-id "$LOG_CMD_ID" --instance-id "$INSTANCE_ID" || true
            aws ssm get-command-invocation \
              --instance-id "$INSTANCE_ID" \
              --command-id "$LOG_CMD_ID" \
              --query "StandardOutputContent" --output text
            exit 1
          fi

      - name: Fetch kubeconfig via SSM
        run: |
          COMMAND_ID=$(aws ssm send-command \
            --instance-ids "$INSTANCE_ID" \
            --document-name "AWS-RunShellScript" \
            --parameters 'commands=["cat /etc/rancher/k3s/k3s.yaml"]' \
            --query "Command.CommandId" --output text)

          aws ssm wait command-executed \
            --command-id "$COMMAND_ID" \
            --instance-id "$INSTANCE_ID" || true

          STATUS=$(aws ssm get-command-invocation \
            --instance-id "$INSTANCE_ID" \
            --command-id "$COMMAND_ID" \
            --query "Status" --output text)

          aws ssm get-command-invocation \
            --instance-id "$INSTANCE_ID" \
            --command-id "$COMMAND_ID" \
            --query "StandardOutputContent" --output text > kubeconfig.yaml

          if [ "$STATUS" != "Success" ] || [ ! -s kubeconfig.yaml ] || ! grep -q "apiVersion" kubeconfig.yaml; then
            echo "ERREUR : la commande SSM a échoué (status: $STATUS)."
            echo "--- stdout ---"
            cat kubeconfig.yaml
            echo "--- stderr de la commande SSM ---"
            aws ssm get-command-invocation \
              --instance-id "$INSTANCE_ID" \
              --command-id "$COMMAND_ID" \
              --query "StandardErrorContent" --output text
            exit 1
          fi

          echo "KUBECONFIG=$PWD/kubeconfig.yaml" >> "$GITHUB_ENV"

      - name: Install Session Manager plugin
        run: |
          curl "https://s3.amazonaws.com/session-manager-downloads/plugin/latest/ubuntu_64bit/session-manager-plugin.deb" -o "session-manager-plugin.deb"
          sudo dpkg -i session-manager-plugin.deb

      - name: Start SSM port-forward tunnel to k3s API
        run: |
          aws ssm start-session \
            --target "$INSTANCE_ID" \
            --document-name AWS-StartPortForwardingSession \
            --parameters '{"portNumber":["6443"],"localPortNumber":["6443"]}' \
            > ssm-tunnel.log 2>&1 &
          echo "SSM_TUNNEL_PID=$!" >> "$GITHUB_ENV"

          for i in $(seq 1 10); do
            if nc -z 127.0.0.1 6443 2>/dev/null; then
              echo "Tunnel SSM établi sur le port 6443."
              break
            fi
            echo "Attente du tunnel SSM... ($i/10)"
            sleep 3
          done

      - name: Wait for cluster
        run: kubectl get nodes --request-timeout=30s

      - name: Install/upgrade Traefik
        run: |
          COMMAND_ID=$(aws ssm send-command \
            --instance-ids "$INSTANCE_ID" \
            --document-name "AWS-RunShellScript" \
            --parameters 'commands=[
              "export KUBECONFIG=/etc/rancher/k3s/k3s.yaml",
              "helm repo add traefik https://traefik.github.io/charts || true",
              "helm repo update",
              "helm upgrade --install traefik traefik/traefik --namespace kube-system --set ports.web.port=80 --set ports.web.hostPort=80 --set ports.websecure.port=443 --set ports.websecure.hostPort=443 --set ports.metrics.port=9101 --set hostNetwork=true --set service.spec.type=ClusterIP --set podSecurityContext.runAsNonRoot=false --set podSecurityContext.runAsUser=0 --set podSecurityContext.runAsGroup=0 --set securityContext.capabilities.drop=null --set securityContext.readOnlyRootFilesystem=false --wait --timeout 120s",
              "kubectl delete pod -n kube-system -l app.kubernetes.io/name=traefik --field-selector=status.phase!=Running --ignore-not-found",
              "kubectl rollout status deployment/traefik -n kube-system --timeout=120s"
            ]' \
            --query "Command.CommandId" --output text)

          echo "Waiting for Traefik install command to complete..."
          aws ssm wait command-executed \
            --command-id "$COMMAND_ID" \
            --instance-id "$INSTANCE_ID" || true

          STATUS=$(aws ssm get-command-invocation \
            --instance-id "$INSTANCE_ID" \
            --command-id "$COMMAND_ID" \
            --query "Status" --output text)

          aws ssm get-command-invocation \
            --instance-id "$INSTANCE_ID" \
            --command-id "$COMMAND_ID" \
            --query "StandardOutputContent" --output text

          if [ "$STATUS" != "Success" ]; then
            echo "ERREUR : l'installation de Traefik a échoué (status: $STATUS)."
            echo "--- stderr de la commande SSM ---"
            aws ssm get-command-invocation \
              --instance-id "$INSTANCE_ID" \
              --command-id "$COMMAND_ID" \
              --query "StandardErrorContent" --output text
            exit 1
          fi

      - name: Verify Traefik is actually serving traffic
        run: |
          COMMAND_ID=$(aws ssm send-command \
            --instance-ids "$INSTANCE_ID" \
            --document-name "AWS-RunShellScript" \
            --parameters 'commands=[
              "for i in $(seq 1 10); do",
              "  CODE=$(curl -s -o /dev/null -w \"%{http_code}\" --max-time 5 http://localhost/ || echo 000);",
              "  if [ \"$CODE\" != \"000\" ]; then echo TRAEFIK_UP code=$CODE; exit 0; fi;",
              "  echo \"Traefik ne répond pas encore sur :80... ($i/10)\";",
              "  sleep 5;",
              "done",
              "echo TRAEFIK_DOWN",
              "exit 1"
            ]' \
            --query "Command.CommandId" --output text)

          aws ssm wait command-executed \
            --command-id "$COMMAND_ID" \
            --instance-id "$INSTANCE_ID" || true

          STATUS=$(aws ssm get-command-invocation \
            --instance-id "$INSTANCE_ID" \
            --command-id "$COMMAND_ID" \
            --query "Status" --output text)

          aws ssm get-command-invocation \
            --instance-id "$INSTANCE_ID" \
            --command-id "$COMMAND_ID" \
            --query "StandardOutputContent" --output text

          if [ "$STATUS" != "Success" ]; then
            echo "ERREUR : Traefik ne répond pas sur le port 80 de l'instance après installation."
            exit 1
          fi

      - name: Apply namespace
        run: envsubst < k8s/namespace.yaml | kubectl apply -f -

      - name: Create/update ConfigMap
        run: |
          kubectl create configmap hrflow-config \
            --namespace ${{ env.NAMESPACE }} \
            --from-literal=NODE_ENV=${{ inputs.environment }} \
            --from-literal=DB_HOST="${{ env.RDS_ADDRESS }}" \
            --from-literal=DB_PORT=5432 \
            --from-literal=DB_NAME="${{ env.DB_NAME }}" \
            --from-literal=AUTH_SERVICE_URL=http://auth:3001 \
            --from-literal=PAIE_SERVICE_URL=http://paie:3002 \
            --from-literal=CONGES_SERVICE_URL=http://conges:3003 \
            --from-literal=RECRUTEMENT_SERVICE_URL=http://recrutement:3004 \
            --from-literal=REDIS_HOST=redis \
            --from-literal=REDIS_PORT=6379 \
            --from-literal=CORS_ALLOWED_ORIGINS="${{ vars.CORS_ALLOWED_ORIGINS }}" \
            --dry-run=client -o yaml | kubectl apply -f -

      - name: Create/update Secret
        run: |
          kubectl create secret generic hrflow-secrets \
            --namespace ${{ env.NAMESPACE }} \
            --from-literal=DB_USER="${{ env.DB_USERNAME }}" \
            --from-literal=DB_PASSWORD="${{ secrets.DB_PASSWORD }}" \
            --from-literal=DATABASE_URL="postgresql://${{ env.DB_USERNAME }}:${{ secrets.DB_PASSWORD }}@${{ env.RDS_ENDPOINT }}/${{ env.DB_NAME }}" \
            --from-literal=JWT_SECRET="${{ secrets.JWT_SECRET }}" \
            --from-literal=JWT_REFRESH_SECRET="${{ secrets.JWT_REFRESH_SECRET }}" \
            --from-literal=STRIPE_SECRET_KEY="${{ secrets.STRIPE_SECRET_KEY }}" \
            --dry-run=client -o yaml | kubectl apply -f -

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@d539f0932e70871a027e9d5a9d8fc38589180a64 # v2

      - name: Create ECR pull secret
        run: |
          ECR_TOKEN=$(aws ecr get-login-password --region "${{ secrets.AWS_REGION }}")

          kubectl create secret docker-registry ecr-pull-secret \
            --namespace "${{ env.NAMESPACE }}" \
            --docker-server="${{ steps.login-ecr.outputs.registry }}" \
            --docker-username=AWS \
            --docker-password="$ECR_TOKEN" \
            --dry-run=client -o yaml | kubectl apply -f -

      - name: Initialize database schema
        run: |
          kubectl apply -f k8s/postgres-init-configmap.yaml -n ${{ env.NAMESPACE }}
          kubectl delete job postgres-init -n ${{ env.NAMESPACE }} --ignore-not-found
          kubectl apply -f k8s/postgres-init-job.yaml -n ${{ env.NAMESPACE }}
          kubectl wait --for=condition=complete job/postgres-init -n ${{ env.NAMESPACE }} --timeout=120s

      - name: Debug database init on failure
        if: failure()
        run: |
          kubectl logs -n ${{ env.NAMESPACE }} job/postgres-init --tail=100 || true

      - name: Deploy Redis
        run: |
          kubectl apply -f k8s/redis/ -n ${{ env.NAMESPACE }}
          kubectl rollout status statefulset/redis -n ${{ env.NAMESPACE }} --timeout=60s

      - name: Substitute image variables and deploy backend services
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          for svc in auth paie conges recrutement; do
            envsubst < k8s/$svc/deployment.yaml | kubectl apply -n ${{ env.NAMESPACE }} -f -
            kubectl apply -f k8s/$svc/service.yaml -n ${{ env.NAMESPACE }}
          done

      - name: Wait for backend services
        run: |
          for svc in auth paie conges recrutement; do
            kubectl rollout status deployment/$svc -n ${{ env.NAMESPACE }} --timeout=120s
          done

      - name: Debug pods on failure
        if: failure()
        run: |
          echo "--- pods (namespace ${{ env.NAMESPACE }}) ---"
          kubectl get pods -n ${{ env.NAMESPACE }} -o wide
          for svc in auth paie conges recrutement; do
            echo "=== $svc : describe deployment ==="
            kubectl describe deployment/$svc -n ${{ env.NAMESPACE }} || true
            echo "=== $svc : describe pods ==="
            kubectl get pods -n ${{ env.NAMESPACE }} -l app=$svc -o name | xargs -r -I{} kubectl describe {}
            echo "=== $svc : logs ==="
            kubectl logs -n ${{ env.NAMESPACE }} -l app=$svc --tail=50 --all-containers || true
          done

      - name: Deploy gateway
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          envsubst < k8s/gateway/deployment.yaml | kubectl apply -n ${{ env.NAMESPACE }} -f -
          kubectl apply -f k8s/gateway/service.yaml -n ${{ env.NAMESPACE }}
          kubectl rollout status deployment/gateway -n ${{ env.NAMESPACE }} --timeout=120s

      - name: Deploy frontend
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          envsubst < k8s/frontend/deployment.yaml | kubectl apply -n ${{ env.NAMESPACE }} -f -
          kubectl apply -f k8s/frontend/service.yaml -n ${{ env.NAMESPACE }}
          kubectl rollout status deployment/frontend -n ${{ env.NAMESPACE }} --timeout=120s

      - name: Apply Ingress
        run: kubectl apply -f k8s/ingress.yaml -n ${{ env.NAMESPACE }}

      - name: Show idle cluster status
        run: |
          echo "Couleur idle ($IDLE_COLOR) déployée sur l'instance $INSTANCE_ID."
          echo "Pas encore de trafic public dessus : bascule faite par le job switch-traffic après smoke-test-idle."

      - name: Close SSM tunnel
        if: always()
        run: kill "$SSM_TUNNEL_PID" || true

  # ============================================================
  # Smoke tests sur la couleur idle, AVANT toute bascule de trafic. On
  # atteint le port 80 de l'instance idle via un tunnel SSM (le security
  # group n'autorise ce port QUE depuis l'ALB, donc le tunnel SSM est le
  # seul moyen d'y accéder directement, même pattern que le tunnel 6443
  # utilisé pour l'API k3s dans le job deploy-idle).
  # ============================================================
  smoke-test-idle:
    name: Smoke test idle color
    runs-on: ubuntu-24.04
    needs: [terraform, deploy-idle]
    environment: ${{ inputs.environment }}
    steps:
      - name: Checkout source
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@7474bc4690e29a8392af63c5b98e7449536d5c3a # v4
        with:
          aws-region: ${{ secrets.AWS_REGION }}
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@b9cd54a3c349d3f38e8881555d616ced269862dd # v3
        with:
          # Le wrapper par défaut ajoute une annotation "::error::Terraform
          # exited with code N." sur STDOUT (pas stderr) dès qu'une commande
          # terraform échoue — ça pollue silencieusement tout `$(terraform
          # output ...)` capturé en shell, même avec `2>/dev/null` (qui ne
          # redirige que stderr). Désactivé partout ici car plusieurs steps
          # dépendent d'une capture stdout propre (ex: fallback "blue" si
          # active_color n'existe pas encore dans le state).
          terraform_wrapper: false

      - name: Get idle instance from state
        working-directory: ./terraform
        run: |
          set -euo pipefail
          terraform init -input=false
          terraform workspace select "${{ inputs.environment }}"

          ACTIVE_COLOR="$(terraform output -raw active_color)"
          if [ "$ACTIVE_COLOR" = "blue" ]; then IDLE_COLOR=green; else IDLE_COLOR=blue; fi
          INSTANCE_ID="$(terraform output -raw "instance_id_${IDLE_COLOR}")"
          if [ -z "$INSTANCE_ID" ]; then
            echo "::error::instance_id_${IDLE_COLOR} est vide." >&2
            exit 1
          fi
          echo "IDLE_COLOR=${IDLE_COLOR}" >> "$GITHUB_ENV"
          echo "INSTANCE_ID=${INSTANCE_ID}" >> "$GITHUB_ENV"

      - name: Install Session Manager plugin
        run: |
          curl "https://s3.amazonaws.com/session-manager-downloads/plugin/latest/ubuntu_64bit/session-manager-plugin.deb" -o "session-manager-plugin.deb"
          sudo dpkg -i session-manager-plugin.deb

      - name: Start SSM port-forward tunnel to idle instance (port 80)
        run: |
          aws ssm start-session \
            --target "$INSTANCE_ID" \
            --document-name AWS-StartPortForwardingSession \
            --parameters '{"portNumber":["80"],"localPortNumber":["18080"]}' \
            > ssm-tunnel.log 2>&1 &
          echo "SSM_TUNNEL_PID=$!" >> "$GITHUB_ENV"

          for i in $(seq 1 10); do
            if nc -z 127.0.0.1 18080 2>/dev/null; then
              echo "Tunnel SSM établi sur le port 18080 -> instance:80."
              break
            fi
            echo "Attente du tunnel SSM... ($i/10)"
            sleep 3
          done

      - name: Smoke test frontend and gateway (via idle instance)
        run: |
          timeout 120 bash -c '
            until curl -fsS http://localhost:18080/ > /dev/null; do
              echo "Waiting for frontend on idle color..."
              sleep 3
            done
          '
          # /api-docs/json agrège le spec OpenAPI du gateway ET des 4 services
          # back (auth/paie/conges/recrutement) : un 200 ici confirme que le
          # gateway répond ET que les 4 services back lui répondent.
          timeout 120 bash -c '
            until curl -fsS http://localhost:18080/api-docs/json > /dev/null; do
              echo "Waiting for API Gateway on idle color..."
              sleep 3
            done
          '
          echo "Smoke tests OK sur la couleur idle ($IDLE_COLOR)."

      - name: Close SSM tunnel
        if: always()
        run: kill "$SSM_TUNNEL_PID" || true

  # ============================================================
  # Bascule atomique du trafic : bouge uniquement le default_action du
  # listener ALB vers la target group idle (in-place, pas de recréation
  # d'instance, pas de downtime). L'ancienne couleur active reste allumée
  # telle quelle : c'est le filet de rollback instantané (workflow
  # rollback.yml, qui refait le même apply avec l'autre couleur).
  # ============================================================
  switch-traffic:
    name: Switch traffic to idle color
    runs-on: ubuntu-24.04
    needs: [terraform, smoke-test-idle]
    environment: ${{ inputs.environment }}
    env:
      TF_VAR_database_password: ${{ secrets.DB_PASSWORD }}
      TF_VAR_ssh_allowed_cidr: ${{ secrets.SSH_ALLOWED_CIDR }}
      TF_VAR_certificate_arn: ${{ vars.ACM_CERTIFICATE_ARN }}
      TF_VAR_alert_email: ${{ vars.ALERT_EMAIL }}

    steps:
      - name: Checkout source
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@7474bc4690e29a8392af63c5b98e7449536d5c3a # v4
        with:
          aws-region: ${{ secrets.AWS_REGION }}
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@b9cd54a3c349d3f38e8881555d616ced269862dd # v3
        with:
          # Le wrapper par défaut ajoute une annotation "::error::Terraform
          # exited with code N." sur STDOUT (pas stderr) dès qu'une commande
          # terraform échoue — ça pollue silencieusement tout `$(terraform
          # output ...)` capturé en shell, même avec `2>/dev/null` (qui ne
          # redirige que stderr). Désactivé partout ici car plusieurs steps
          # dépendent d'une capture stdout propre (ex: fallback "blue" si
          # active_color n'existe pas encore dans le state).
          terraform_wrapper: false

      - name: Terraform init & select workspace
        working-directory: ./terraform
        run: |
          terraform init -input=false
          terraform workspace select "${{ inputs.environment }}"

      - name: Determine idle color
        working-directory: ./terraform
        run: |
          set -euo pipefail
          ACTIVE_COLOR="$(terraform output -raw active_color)"
          if [ "$ACTIVE_COLOR" = "blue" ]; then IDLE_COLOR=green; else IDLE_COLOR=blue; fi
          echo "Bascule : $ACTIVE_COLOR -> $IDLE_COLOR"
          echo "IDLE_COLOR=${IDLE_COLOR}" >> "$GITHUB_ENV"

      - name: Switch ALB listener to idle color
        working-directory: ./terraform
        run: |
          terraform apply -auto-approve \
            -var-file="${{ inputs.environment }}.tfvars" \
            -var="active_color=${IDLE_COLOR}"

      - name: Verify switch via public ALB
        working-directory: ./terraform
        run: |
          set -euo pipefail
          ALB_DNS_NAME="$(terraform output -raw alb_dns_name)"
          timeout 60 bash -c "
            until curl -fsS http://${ALB_DNS_NAME}/ > /dev/null; do
              echo 'Waiting for ALB to serve the new active color...'
              sleep 3
            done
          "
          echo "Bascule confirmée : l'ALB (${ALB_DNS_NAME}) sert désormais la couleur ${IDLE_COLOR}."
