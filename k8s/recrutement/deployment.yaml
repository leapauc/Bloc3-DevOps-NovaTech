# k8s/recrutement/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: recrutement
  namespace: hrflow
spec:
  replicas: 2
  selector:
    matchLabels:
      app: recrutement
  template:
    metadata:
      labels:
        app: recrutement
    spec:
      imagePullSecrets:
        - name: ecr-pull-secret
      containers:
        - name: recrutement
          image: ${ECR_REGISTRY}/hrflow/recrutement:${IMAGE_TAG}
          ports:
            - containerPort: 3004
          envFrom:
            - configMapRef:
                name: hrflow-config
            - secretRef:
                name: hrflow-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests: { cpu: "100m", memory: "128Mi" }
            limits: { cpu: "500m", memory: "512Mi" }