// Module de matching CV — WIP Camille (mars 2024)
// Jamais terminé — trop complexe, pas eu le temps
// TODO: implémenter l'algo NLP un jour

function scoreCandidat(candidat, offre) {
  // Placeholder — retourne un score aléatoire
  // L'algo réel nécessiterait du NLP sur le CV
  return Math.random()
}

module.exports = { scoreCandidat }
